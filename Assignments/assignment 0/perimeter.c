void main()
{
    int length, width, perimeter;
    printf("Enter Length: ");
    scanf("%d", &length);

    printf("Enter Width: ");
    scanf("%d", &width);

    perimeter = 2 * (length + width);

    printf("Perimeter = %d", perimeter);

    
}